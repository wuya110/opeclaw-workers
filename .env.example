# Cloudflare / Wrangler
# 先通过 wrangler login 或环境变量完成 Cloudflare 认证

# Worker 可用变量
APP_NAME=院生 AI 实验站
ALLOW_ORIGIN=*
HF_MODEL=Qwen/Qwen2.5-72B-Instruct

# 敏感信息不要直接提交到仓库，使用：
# wrangler secret put HF_TOKEN
# wrangler secret put GITHUB_TOKEN
